# ⏱️ 50分タイムテーブル「ガチャを作ろう！」

- 実施：10月5日／50分1コマ／10クラス共通
- 進め方：`lesson_video.pptx` から作った動画（ナレーション部分は約14分）を再生する。生徒は ⏸ マークで一時停止してノートブックで作業する。
- 教材：`lesson.ipynb`（Colab）・`video_script.md`（ナレーション原稿／スクショ撮影リスト）

## タイムテーブル

| 時間 | 区分 | 内容 | 分 | 動画スライド |
|---|---|---|:---:|---|
| 0:00–0:05 | 導入 | ゴール提示（完成デモ）・8ステップの流れ・動画の使い方・iPadのスマート句読点オフ | 5 | 1–5 |
| 0:05–0:08 | 準備 | Colabを開く → 準備セル実行（警告 →「このまま実行」）・エラー対処の紹介 | 3 | 6–8 |
| 0:08–0:11 | Step1 | 🎲 変数：`r = random.random()` を `print` | 3 | 9–10 |
| 0:11–0:15 | Step2 | 🔀 if：`if r <= 0.6:` の1分岐 | 4 | 11–12 |
| 0:15–0:21 | Step3 | 🔀 elif / else：4段階に拡張 | 6 | 13–14 |
| 0:21–0:25 | Step4 | 🔁 for：`for i in range(5):` で5回 | 4 | 15–16 |
| 0:25–0:30 | Step5 | 📦 リスト：`results = []`／`append`／`print(results)` | 5 | 17–18 |
| 0:30–0:36 | Step6 | 🧰 関数：`def gacha(count):`／`return results`／`print(gacha(10))` | 6 | 19–20 |
| 0:36–0:41 | Step7 | 🃏 アプリ化：`card(...)` に差し替え → `return gallery(results)` → `run_app(gacha, ...)` で完成 🎉 | 5 | 21–23 |
| 0:41–0:46 | Step8 | 🎨 アレンジタイム：しきい値・名前・色・絵文字を書きかえ（早い人は5段階に） | 5 | 24–25 |
| 0:46–0:48 | まとめ | 順次・分岐・反復のふりかえり＋ふりかえり問い（UR を1%にするには？） | 2 | 26 |
| 0:48–0:50 | バッファ | 遅れの吸収・保存（「ドライブにコピーを保存」） | 2 | — |
| | | **合計** | **50** | |

## ステップ構成について（CLAUDE.md のたたき台からの変更点）

**ステップの統合はしていない**（Step1〜8 の8ステップのまま）。ただし Step6 と Step7 の中身の分け方を変えた。

| | たたき台 | 今回の構成 |
|---|---|---|
| Step6 | 関数化 ＋ `card` に差し替え ＋ `return gallery(...)` | **関数化だけ**（`def` / `return results` / `int(count)`）。`print(gacha(10))` でリストが出れば成功 |
| Step7 | `run_app` に接続 | **`card` 差し替え ＋ `gallery` ＋ `run_app`** をまとめて完成まで |

**理由**
- たたき台の Step6 には `def`・`return`・`card`・`gallery` の4つが新しく出てくる。「1ステップに新出は1つまで」の方針にぎりぎり収まらない。
- `card(...)` の結果（HTML文字列）を `print` すると、生徒にHTMLタグが見えてしまう。方針3「HTMLは見せない」に反するので、`card` を入れるのは画面表示まで一気にできる `run_app` と同じステップにした。
- Step7 の `card` / `gallery` / `run_app` は3つとも「準備セルの道具を使う」という1つの新しい要素として扱う。4行ある `card(...)` への書きかえは最初から入れてあり、生徒が埋める空欄は2つ（`gallery` と `gacha`）だけ。

## iPad向けの工夫（時間を守るため）

- Step4 以降は**字下げ済みのコード**を渡し、生徒は `___` を埋めるだけにした。iPadで何行も字下げし直すのは時間がかかり、ミスも起きやすいため。
- 各ステップの下に **「🆘 こたえ」（折りたたみ）** を置いた。遅れた生徒はコピーして次のステップに進める。どのセルも単体で実行できる。
- 冒頭でスマート句読点をオフにして、`'` が `’` に化ける SyntaxError を防ぐ。

## 遅れたときの調整（優先順）

1. **Step8 を短くする**（最低2分）。色や名前を1か所だけ変えれば十分。
2. **Step3 の「UR が出るまで連打」を省く**（-1分）
3. **Step4 と Step5 を続けて見せる**。Step4 の実行確認を省いて Step5 の空欄だけ埋めさせる（-2分）。
4. まとめのふりかえり問いは宿題にしてもよい

## クラス間で統一すること

- 動画は全クラス同じものを使う（ナレーションはノートの原稿と同じ。口調は「〜だよ」「〜しよう」で統一）
- 授業の最初の1分で「⏸マークで止める」「困ったら🆘こたえ」の2点を口頭でも伝えると、その後の流れが安定しやすい
